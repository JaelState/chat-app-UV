// components/Header.js
import React from 'react';
import { Typography } from '@mui/material';

const Header = () => {
    return (
        <Typography variant="h1" sx={{ color: 'red', fontSize: '48px', textAlign: 'center', marginTop: '20px' }}>Spook</Typography>
    );
};

export default Header;
